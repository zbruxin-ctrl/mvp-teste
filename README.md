# MVP Automação Local

## 🚀 Como Rodar

```bash
# 1. Instalar dependências
npm install

# 2. Rodar em modo desenvolvimento (build + watch + server)
npm run dev

# 3. Ou build e rodar separado
npm run build
npm run serve